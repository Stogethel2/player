export interface detail {
    header_color_class: string;
    overlay_image: boolean;
    header_image_background: string;
    icon: string;
    title: string;
    time: string;
    detail: string;
    button : string;
    name : string;
}

export interface playlists {
    list: detail[];
}