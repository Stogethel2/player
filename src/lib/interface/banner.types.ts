export type Response = {
    id: string;
    agent_id: string;
    description: string;
    image: string;
    sequence: number;
    is_active: boolean;
    created_at: string;
    updated_at: string;
}